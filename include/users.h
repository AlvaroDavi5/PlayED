#ifndef _USERS_H

	#define _USERS_H

	#include "playlists.h"
	#include "friendship.h"

	/* type definitions */
	typedef struct users_list UsersList;
	typedef struct usr User;

	/* function prototypes */
	int noneUS();

#endif
